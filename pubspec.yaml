name: agerelige_flutter_client
description: Flutter client for existing AgereLige AWS backend (migrated from iOS).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"


dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  cached_network_image: ^3.4.1
  flutter_hooks: ^0.20.5
  hooks_riverpod: ^2.5.1
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0     # Optional - if using auth
  cloud_firestore: ^5.6.0    # Optional - if using Firestore
  firebase_messaging: ^15.5.0  # Optional - if using push notifications
  firebase_analytics: ^11.6.0  # Optional - if using analytics"
  dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
